{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "corepack enable && pnpm install --frozen-lockfile && pnpm --filter sgh-backend exec prisma generate && pnpm --filter sgh-backend build"
  },
  "deploy": {
    "startCommand": "pnpm --filter sgh-backend exec prisma migrate deploy && pnpm --filter sgh-backend start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
